{
  "name": "connect-four",
  "version": "1.0.0",
  "description": "Connect Four game built with React",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "startmon": "nodemon server.js",
    "babel": "npx babel client --out-dir client/compiled --ignore client/compiled --source-maps inline",
    "babelwatch": "npx babel client --watch --out-dir client/compiled --ignore client/compiled --source-maps inline"
  },
  "author": "Moriah Rahamim",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "nodemon": "^1.14.12"
  }
}
