{
  "name": "bowling-with-react",
  "version": "1.0.0",
  "description": "bowling score tracker built with express and react",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "babelwatch": "npx babel client --watch --out-dir client/compiled --ignore client/compiled --source-maps inline",
    "babel": "npx babel client --out-dir client/compiled --ignore client/compiled --source-maps inline"
  },
  "author": "moriah rahamim",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1",
    "live-server": "^1.2.0",
    "nodemon": "^1.14.12"
  }
}
